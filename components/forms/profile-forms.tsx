"use-client"
import React, { useState } from 'react'

type Props = {}

export const ProfileForm = (props: Props) => {
    const [isLoading, setLoading] = useState(false)
    return (
        <div>profile-forms</div>
    )
}